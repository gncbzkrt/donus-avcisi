name: Günlük BIST Tarama
on:
  schedule:
    - cron: '15 16 * * 1-5'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - run: python scripts/update_universe.py
      - run: python -m engine.run_scan
      - name: Sonuçları kaydet
        run: |
          git config user.name 'reversal-hunter-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add data/universe.json data/latest.json data/performance.json data/history
          git diff --cached --quiet || git commit -m "Günlük tarama $(date -u +%F)"
          git push
